// Default for `ng build` (production). Point this at your deployed API (no trailing slash).
export const environment = {
  production: true,
  apiBaseUrl: 'https://backend-jet-seven-76.vercel.app',
};
